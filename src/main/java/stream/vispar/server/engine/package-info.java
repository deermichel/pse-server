/**
 * Encapsulates the deployment and undeployment of CEP patterns.
 * 
 * <p>
 * The package provides a simple interface
 * ({@link stream.vispar.server.engine.IEngine}) that is used for communication
 * with the CEP engine.
 */
package stream.vispar.server.engine;